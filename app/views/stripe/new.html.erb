<h1>Your order <%= @payment.booking_sku %></h1>
<%= form_tag payment_stripe_path(@payment) do %>
  <article>
    <label class="amount">
      <span>Amount: <%= humanized_money_with_symbol(@payment.amount) %></span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-name="My order"
    data-email="<%= current_user.email %>"
    data-description="Place<%= @payment.booking_sku %>"
    data-amount="<%= @payment.amount_cents %>"
    data-currency="<%= @payment.amount.currency %>"></script>
  -->
<% end %>
